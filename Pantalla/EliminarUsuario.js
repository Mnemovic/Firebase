import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const detalleAutos = (props) => {
    return (
      <View>
        console.log()
        <Text> Eliminar Usuarios</Text>
      </View>
    );
  }

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });

  export default detalleAutos;